"""Agentic evaluation harness (runtime code).

Populated starting Stage 19. Not to be confused with the top-level
`eval/` directory at the repo root, which holds the Stage-1 *data*
(gold answers, rubric, dataset validator) this harness will run against —
see eval/rubric.md for the scoring logic this package will implement, and
eval/gold/ for what it scores against. Also wired into Stage 20
(adversarial testing) and Stage 21 (observability/cost reporting).
"""
